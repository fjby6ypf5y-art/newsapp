{
  "name": "newsapp",
  "private": true,
  "description": "A news reader that lives on the phone. The app itself is index.html and has no dependencies; this file exists only so the tests can be installed.",
  "scripts": {
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
